import React from 'react';
import '../styles/PageLayout.css';
interface Props {
    children: React.ReactNode;
    title?: string;
    buttonBack?: boolean;
}
export declare const PageLayout: ({ children, title, buttonBack }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
